{
  "name": "nyx-guard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/build-content.js && node scripts/copy-static.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.259",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5",
    "vite": "^5.4.2"
  }
}
